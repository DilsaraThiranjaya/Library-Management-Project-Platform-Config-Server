server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DilsaraThiranjaya/Library-Management-Project-Configurations.git
          default-label: main
          search-paths: 'platform,services'
          clone-on-start: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false